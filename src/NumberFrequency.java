import java.util.*;
import java.util.stream.Collectors;

public class NumberFrequency {
    public static void main(String[] args) {
        List<Integer> list = Arrays.asList(4,6,1,6,3,4,5,6,1,1,2,3);
       // HashMap<Integer,Integer> mapList = list.stream().filter(e->Collections.frequency(list,e)>1).collect(Collectors.toMap());
    }
}
